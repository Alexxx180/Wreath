<UserControl x:Class="Wreath.Controls.Tables.Specialities.ProfessionalCompetetions.ProfessionalCompetetionRow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             DataContext="{Binding RelativeSource={RelativeSource Self}}"
             d:DesignHeight="35" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="Impact1" BasedOn="{StaticResource ImpactButton3}" TargetType="Button" />
        <Style x:Key="Impact2" BasedOn="{StaticResource ImpactButton4}" TargetType="Button" />
    </UserControl.Resources>
    <Button Height="35" HorizontalContentAlignment="Stretch" Click="Select">
        <Button.IsEnabled>
            <Binding Path="IsMarked" />
        </Button.IsEnabled>
        <Button.Style>
            <Binding Path="Selection" />
        </Button.Style>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.1*" />
                <ColumnDefinition Width="0.1*" />
                <ColumnDefinition Width="0.1*" />
                <ColumnDefinition Width="0.25*" />
                <ColumnDefinition Width="0.15*" />
                <ColumnDefinition Width="0.15*" />
                <ColumnDefinition Width="0.15*" />
            </Grid.ColumnDefinitions>

            <Viewbox IsHitTestVisible="False">
                <TextBlock Text="{Binding Path=No}" />
            </Viewbox>

            <Viewbox IsHitTestVisible="False" Grid.Column="1">
                <TextBlock Text="{Binding Path=ProfessionalNo1}" />
            </Viewbox>

            <Viewbox IsHitTestVisible="False" Grid.Column="2">
                <TextBlock Text="{Binding Path=ProfessionalNo2}" />
            </Viewbox>

            <Viewbox IsHitTestVisible="False" Grid.Column="3">
                <TextBlock Text="{Binding Path=ProfessionalName}" />
            </Viewbox>

            <Viewbox IsHitTestVisible="False" Grid.Column="4">
                <TextBlock Text="{Binding Path=Experience}" />
            </Viewbox>

            <Viewbox IsHitTestVisible="False" Grid.Column="5">
                <TextBlock Text="{Binding Path=Skills}" />
            </Viewbox>

            <Viewbox IsHitTestVisible="False" Grid.Column="6">
                <TextBlock Text="{Binding Path=Knowledge}" />
            </Viewbox>
        </Grid>
    </Button>
</UserControl>